# Catalog — the no.fail site

Maps the nofail-jidoka site subtree, pinned at commit `16a4fc4`. Ten nouns, ten
cards, every claim cited as `path:line@16a4fc4`. **Load this catalog, then open
one card — never the whole cards folder, never the territory itself.** The
inventory found no leftovers: every nominated file is reached by an edge or by a
named contract, so this territory carries no dead weight.

## Layout spine

- **Base layout** (live) — why every page has the same head tags, nav and footer, and where a page's title comes from → `cards/base-layout.md`

## Token and style source

- **Global styles** (live) — every colour, typeface and spacing step on the site resolves here → `cards/global-styles.md`

## Components

- **Nav** (live) — the top bar's six destinations, and how the current page gets marked → `cards/nav.md`
- **Footer** (live) — the bottom bar's link list, the standing verified line, and the year → `cards/footer.md`
- **ContactForm** (live) — what a visitor fills in, and what still works when their JavaScript is off → `cards/contact-form.md`
- **RigDemo** (live) — the homepage widget that acts out a pipeline run, and what it shows when nothing can run → `cards/rig-demo.md`

## Content collections

- **Content collections config** (live) — which fields a writing, work or people entry is allowed to have, and where each kind is read from → `cards/content-config.md`

## Site config

- **Astro config** (live) — the site's public origin, and why the build emits plain files → `cards/astro-config.md`
- **package.json** (live) — which command builds the site, and what the build is allowed to reach for → `cards/package-json.md`

## Serverless functions

- **Contact function** (live) — where a submitted message actually goes, and where the destination address is kept → `cards/contact-function.md`

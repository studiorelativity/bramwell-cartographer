<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" width="32" height="32" role="img" aria-label="no.fail">
  <!-- design-system.md § Favicon: underscore block in --accent on --ink, 32px
       grid. The cursor is the mark. Bar proportions are the lockup cursor's
       (0.5em wide x 0.12em tall), bottom-aligned to the baseline as it is in
       the lockup. An SVG asset carries its literals: it is not a stylesheet
       and cannot reference a custom property. -->
  <rect width="32" height="32" fill="#111110"/>
  <rect x="8" y="19" width="16" height="4" fill="#FF7518"/>
</svg>

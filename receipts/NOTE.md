These transcripts are unedited output of _rig/bin/walk-test.sh against
the final map (run 2026-08-19_223934, territory commit 16a4fc4), one
cold headless session per question, able to read only catalog.md and
cards/. Both model sets walked the identical map. One known
overstatement is preserved rather than hidden: haiku-q2 asserts enum
values the card does not contain (the real values are in the schema
lines the card cites — which is what makes the overstatement
checkable). The frontier set reported the same card's boundary instead.
The contrast is the exhibit.

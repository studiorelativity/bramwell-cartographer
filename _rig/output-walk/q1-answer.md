All colors, typefaces, and spacing come from `src/styles/global.css`, loaded site-wide via `src/layouts/Base.astro`; it's the one file allowed to use hex literals, with the pumpkin accent color defined at `src/styles/global.css:72` inside the `:root` token block — that's the file to edit. Card read: **cards/global-styles.md**.
model: claude-sonnet-5
